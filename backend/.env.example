# Log level filter (tracing directives)
RUST_LOG=docx_jast_backend=debug,tower_http=info

# Log format: "pretty" (default, colored, human-readable) or "json" (structured, for production)
LOG_FORMAT=pretty
